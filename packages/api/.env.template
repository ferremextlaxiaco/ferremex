STORE_CORS=http://localhost:8000,https://docs.medusajs.com
ADMIN_CORS=http://localhost:7000,http://localhost:9000
VENDOR_CORS=http://localhost:7001
AUTH_CORS=http://localhost:7000,http://localhost:7001,http://localhost:9000
REDIS_URL=redis://localhost:6379
JWT_SECRET=supersecret
COOKIE_SECRET=supersecret
DATABASE_URL=

# Token compartido del POS: exigido en métodos mutantes de /caja/* (excepto /caja/login).
# Si se deja vacío, la validación se desactiva (modo dev). En el cliente: VITE_POS_TOKEN.
POS_TOKEN=
# Token para la vista admin de usuarios que incluye PINs (GET /caja/usuarios?admin=1).
# En el cliente: VITE_POS_ADMIN_TOKEN.
POS_ADMIN_TOKEN=

